@model ProductsPageViewModel;

@{
    var product = Model.Product;
    var products = Model.Products;
    ViewData["Title"] = "Products";
}

<section>
    <h1 class="display-4 mb-3">DojoShop© Products</h1>
    <div class="row">
        <div class="col-12 col-md-6">
            <h2 class="display-6 mb-3">All Products</h2>
            @await Html.PartialAsync("_AllProducts", products)
        </div>
        <div class="col-12 col-md-6">
            <h2 class="display-6 mb-3">Add a Product</h2>
            @await Html.PartialAsync("_AddAProduct", product)
        </div>
    </div>
</section>

@section Scripts {
    <script src="~/js/focus-input.js"></script>
}
